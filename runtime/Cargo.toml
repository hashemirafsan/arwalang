[package]
name = "arwa_runtime"
version = "0.1.0"
edition = "2021"

[lib]
name = "arwa_runtime"
path = "src/lib.rs"
crate-type = ["rlib", "staticlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
